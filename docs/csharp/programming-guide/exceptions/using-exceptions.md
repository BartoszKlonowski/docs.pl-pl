---
title: Korzystanie z wyjątków — Przewodnik programowania w języku C#
description: Dowiedz się, jak używać wyjątków. Wyjątki są zgłaszane przez kod, który napotka błąd i przechwycony przez kod, który koryguje błąd.
ms.date: 12/09/2020
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
ms.openlocfilehash: 30a7c3eecb599493dfa74d664c4899836c1b9276
ms.sourcegitcommit: 9b877e160c326577e8aa5ead22a937110d80fa44
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/11/2020
ms.locfileid: "97110224"
---
# <a name="use-exceptions-c-programming-guide"></a><span data-ttu-id="b6f24-104">Używanie wyjątków (Przewodnik programowania w języku C#)</span><span class="sxs-lookup"><span data-stu-id="b6f24-104">Use exceptions (C# programming guide)</span></span>

<span data-ttu-id="b6f24-105">W języku C# błędy w programie w czasie wykonywania są propagowane za pośrednictwem programu przy użyciu mechanizmu nazywanego wyjątkami.</span><span class="sxs-lookup"><span data-stu-id="b6f24-105">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="b6f24-106">Wyjątki są zgłaszane przez kod, który napotka błąd i przechwycony przez kod, który może poprawić błąd.</span><span class="sxs-lookup"><span data-stu-id="b6f24-106">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="b6f24-107">Wyjątki mogą być zgłaszane przez środowisko uruchomieniowe .NET lub kod w programie.</span><span class="sxs-lookup"><span data-stu-id="b6f24-107">Exceptions can be thrown by the .NET runtime or by code in a program.</span></span> <span data-ttu-id="b6f24-108">Gdy wyjątek jest zgłaszany, propaguje stos wywołań do momentu `catch` znalezienia instrukcji dla wyjątku.</span><span class="sxs-lookup"><span data-stu-id="b6f24-108">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="b6f24-109">Nieprzechwycone wyjątki są obsługiwane przez ogólną procedurę obsługi wyjątków dostarczoną przez system, który wyświetla okno dialogowe.</span><span class="sxs-lookup"><span data-stu-id="b6f24-109">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>

<span data-ttu-id="b6f24-110">Wyjątki są reprezentowane przez klasy pochodne od <xref:System.Exception> .</span><span class="sxs-lookup"><span data-stu-id="b6f24-110">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="b6f24-111">Ta klasa identyfikuje typ wyjątku i zawiera właściwości, które zawierają szczegółowe informacje o wyjątku.</span><span class="sxs-lookup"><span data-stu-id="b6f24-111">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="b6f24-112">Zgłaszanie wyjątku obejmuje utworzenie wystąpienia klasy pochodnej wyjątku, opcjonalnie skonfigurowanie właściwości wyjątku, a następnie wygenerowanie obiektu za pomocą `throw` słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="b6f24-112">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="b6f24-113">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="b6f24-113">For example:</span></span>

:::code language="csharp" source="snippets/exceptions/Program.cs" id="ThrowException":::

<span data-ttu-id="b6f24-114">Po zgłoszeniu wyjątku środowisko uruchomieniowe sprawdza bieżącą instrukcję, aby sprawdzić, czy znajduje się w `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="b6f24-114">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="b6f24-115">Jeśli tak jest, wszystkie `catch` bloki skojarzone z `try` blokiem są sprawdzane w celu sprawdzenia, czy mogą przechwytywać wyjątek.</span><span class="sxs-lookup"><span data-stu-id="b6f24-115">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="b6f24-116">`Catch` bloki zwykle określają typy wyjątków; Jeśli typ `catch` bloku jest tego samego typu co wyjątek lub Klasa bazowa wyjątku, `catch` blok może obsłużyć metodę.</span><span class="sxs-lookup"><span data-stu-id="b6f24-116">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="b6f24-117">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="b6f24-117">For example:</span></span>

:::code language="csharp" source="snippets/exceptions/Program.cs" id="CatchException":::

<span data-ttu-id="b6f24-118">Jeśli instrukcja zwracająca wyjątek nie znajduje się w `try` bloku lub jeśli `try` blok, który go zawiera, nie ma pasującego `catch` bloku, środowisko uruchomieniowe sprawdza metodę wywołującą dla `try` instrukcji i `catch` bloków.</span><span class="sxs-lookup"><span data-stu-id="b6f24-118">If the statement that throws an exception isn't within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="b6f24-119">Środowisko uruchomieniowe kontynuuje wywoływanie stosu, wyszukując zgodny `catch` blok.</span><span class="sxs-lookup"><span data-stu-id="b6f24-119">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="b6f24-120">Po `catch` znalezieniu i wykonaniu bloku, kontrola jest przenoszona do następnej instrukcji po tym `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="b6f24-120">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>

<span data-ttu-id="b6f24-121">`try`Instrukcja może zawierać więcej niż jeden `catch` blok.</span><span class="sxs-lookup"><span data-stu-id="b6f24-121">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="b6f24-122">Pierwsza `catch` instrukcja, która może obsłużyć wyjątek, jest wykonywana; wszelkie poniższe `catch` instrukcje, nawet jeśli są zgodne, są ignorowane.</span><span class="sxs-lookup"><span data-stu-id="b6f24-122">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they're compatible, are ignored.</span></span> <span data-ttu-id="b6f24-123">Bloki catch powinny zawsze być uporządkowane z najbardziej konkretnych (lub najbardziej pochodnych) do najmniej określonych.</span><span class="sxs-lookup"><span data-stu-id="b6f24-123">Catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="b6f24-124">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="b6f24-124">For example:</span></span>

:::code language="csharp" source="snippets/exceptions/CatchOrder.cs":::

<span data-ttu-id="b6f24-125">Przed `catch` wykonaniem bloku program sprawdza bloki w czasie wykonywania `finally` .</span><span class="sxs-lookup"><span data-stu-id="b6f24-125">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="b6f24-126">`Finally` bloki umożliwiają programistom czyszczenie dowolnego niejednoznacznego stanu, który może być pozostawiony przez przerwany `try` blok, lub do zwalniania zasobów zewnętrznych (takich jak uchwyty grafiki, połączenia z bazami danych lub strumienie plików) bez czekania na zakończenie działania modułu wyrzucania elementów bezużytecznych w środowisku uruchomieniowym.</span><span class="sxs-lookup"><span data-stu-id="b6f24-126">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections, or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="b6f24-127">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="b6f24-127">For example:</span></span>

:::code language="csharp" source="snippets/exceptions/Program.cs" id="TestFinally":::

<span data-ttu-id="b6f24-128">Jeśli `WriteByte()` wywołał wyjątek, kod w drugim `try` bloku, który próbuje ponownie otworzyć plik, będzie kończyć się niepowodzeniem `file.Close()` , jeśli nie zostanie wywołany, a plik pozostanie zablokowany.</span><span class="sxs-lookup"><span data-stu-id="b6f24-128">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` isn't called, and the file would remain locked.</span></span> <span data-ttu-id="b6f24-129">Ponieważ `finally` bloki są wykonywane nawet w przypadku zgłoszenia wyjątku, `finally` blok w poprzednim przykładzie umożliwia poprawne zamknięcie pliku i pomaga uniknąć błędu.</span><span class="sxs-lookup"><span data-stu-id="b6f24-129">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>

<span data-ttu-id="b6f24-130">Jeśli nie `catch` odnaleziono zgodnego bloku w stosie wywołań po zgłoszeniu wyjątku, wystąpi jedno z trzech rzeczy:</span><span class="sxs-lookup"><span data-stu-id="b6f24-130">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>

- <span data-ttu-id="b6f24-131">Jeśli wyjątek znajduje się w obrębie finalizatora, finalizator zostanie przerwany i zostanie wywołany podstawowy finalizator, jeśli istnieje.</span><span class="sxs-lookup"><span data-stu-id="b6f24-131">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>
- <span data-ttu-id="b6f24-132">Jeśli stos wywołań zawiera statyczny Konstruktor lub zostaje zgłoszony inicjator pola statycznego, <xref:System.TypeInitializationException> z pierwotnym wyjątkiem przypisanym do <xref:System.Exception.InnerException%2A> właściwości nowego wyjątku.</span><span class="sxs-lookup"><span data-stu-id="b6f24-132">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>
- <span data-ttu-id="b6f24-133">Jeśli początek wątku zostanie osiągnięty, wątek zostanie zakończony.</span><span class="sxs-lookup"><span data-stu-id="b6f24-133">If the start of the thread is reached, the thread is terminated.</span></span>
