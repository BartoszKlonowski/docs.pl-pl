---
title: 'CA1062: Weryfikuj argumenty metod publicznych (analiza kodu)'
description: 'Dowiedz się więcej o regule analizy kodu CA1062: Weryfikuj argumenty metod publicznych'
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
- Validate arguments of public methods
helpviewer_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
author: gewarren
ms.author: gewarren
dev_langs:
- CSharp
- VB
ms.openlocfilehash: 4f0194c1ff7f82521f8295080b503729aa97e4ba
ms.sourcegitcommit: e301979e3049ce412d19b094c60ed95b316a8f8c
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/16/2020
ms.locfileid: "97593776"
---
# <a name="ca1062-validate-arguments-of-public-methods"></a><span data-ttu-id="03905-103">CA1062: Waliduj argumenty metod publicznych</span><span class="sxs-lookup"><span data-stu-id="03905-103">CA1062: Validate arguments of public methods</span></span>

| | <span data-ttu-id="03905-104">Wartość</span><span class="sxs-lookup"><span data-stu-id="03905-104">Value</span></span> |
|-|-|
| <span data-ttu-id="03905-105">**Identyfikator zasady**</span><span class="sxs-lookup"><span data-stu-id="03905-105">**Rule ID**</span></span> |<span data-ttu-id="03905-106">CA1062</span><span class="sxs-lookup"><span data-stu-id="03905-106">CA1062</span></span>|
| <span data-ttu-id="03905-107">**Kategoria**</span><span class="sxs-lookup"><span data-stu-id="03905-107">**Category**</span></span> |<span data-ttu-id="03905-108">Microsoft. Design</span><span class="sxs-lookup"><span data-stu-id="03905-108">Microsoft.Design</span></span>|
| <span data-ttu-id="03905-109">**Naprawa jest przerywana lub nieprzerwana**</span><span class="sxs-lookup"><span data-stu-id="03905-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="03905-110">Nieprzerwanie</span><span class="sxs-lookup"><span data-stu-id="03905-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="03905-111">Przyczyna</span><span class="sxs-lookup"><span data-stu-id="03905-111">Cause</span></span>

<span data-ttu-id="03905-112">Widoczna na zewnątrz Metoda odwołuje się do jednego z argumentów odwołania bez weryfikowania, czy ten argument jest `null` ( `Nothing` w Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="03905-112">An externally visible method dereferences one of its reference arguments without verifying whether that argument is `null` (`Nothing` in Visual Basic).</span></span>

<span data-ttu-id="03905-113">Następujące aspekty tej reguły [można konfigurować](#configure-code-to-analyze):</span><span class="sxs-lookup"><span data-stu-id="03905-113">The following aspects of this rule are [configurable](#configure-code-to-analyze):</span></span>

- <span data-ttu-id="03905-114">Określa, czy należy analizować metodę rozszerzenia "This".</span><span class="sxs-lookup"><span data-stu-id="03905-114">Whether or not to analyze extension method 'this' parameter.</span></span>
- <span data-ttu-id="03905-115">Określ metody sprawdzania poprawności sprawdzenia wartości null w przywoływanych bibliotekach lub projektach, które sprawdzają, czy argumenty przekazane do niego są inne niż null.</span><span class="sxs-lookup"><span data-stu-id="03905-115">Specify null check validation methods in referenced libraries or projects, which validate that arguments passed to it are non-null.</span></span>
- <span data-ttu-id="03905-116">Części bazy kodu do wykluczenia z analizy.</span><span class="sxs-lookup"><span data-stu-id="03905-116">Parts of codebase to exclude from analysis.</span></span>

## <a name="rule-description"></a><span data-ttu-id="03905-117">Opis reguły</span><span class="sxs-lookup"><span data-stu-id="03905-117">Rule description</span></span>

<span data-ttu-id="03905-118">Wszystkie argumenty odwołania, które są przekazane do widocznych zewnętrznie metod, powinny być sprawdzane względem `null` .</span><span class="sxs-lookup"><span data-stu-id="03905-118">All reference arguments that are passed to externally visible methods should be checked against `null`.</span></span> <span data-ttu-id="03905-119">W razie potrzeby throw, <xref:System.ArgumentNullException> gdy argument ma wartość `null` .</span><span class="sxs-lookup"><span data-stu-id="03905-119">If appropriate, throw a <xref:System.ArgumentNullException> when the argument is `null`.</span></span>

<span data-ttu-id="03905-120">Jeśli metodę można wywołać z nieznanego zestawu, ponieważ jest ona zadeklarowana jako publiczna lub chroniona, należy zweryfikować wszystkie parametry metody.</span><span class="sxs-lookup"><span data-stu-id="03905-120">If a method can be called from an unknown assembly because it is declared public or protected, you should validate all parameters of the method.</span></span> <span data-ttu-id="03905-121">Jeśli metoda jest przeznaczona do wywoływania tylko przez znane zestawy, należy wprowadzić metodę wewnętrzną i zastosować <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> atrybut do zestawu, który zawiera metodę.</span><span class="sxs-lookup"><span data-stu-id="03905-121">If the method is designed to be called only by known assemblies, you should make the method internal and apply the <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> attribute to the assembly that contains the method.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="03905-122">Jak naprawić naruszenia</span><span class="sxs-lookup"><span data-stu-id="03905-122">How to fix violations</span></span>

<span data-ttu-id="03905-123">Aby naprawić naruszenie tej reguły, sprawdź poprawność każdego argumentu odwołania względem `null` .</span><span class="sxs-lookup"><span data-stu-id="03905-123">To fix a violation of this rule, validate each reference argument against `null`.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="03905-124">Kiedy pominąć ostrzeżenia</span><span class="sxs-lookup"><span data-stu-id="03905-124">When to suppress warnings</span></span>

<span data-ttu-id="03905-125">Możesz pominąć ostrzeżenie z tej reguły, jeśli masz pewność, że odwołanie do parametru zostało zweryfikowane przez inne wywołanie metody w funkcji.</span><span class="sxs-lookup"><span data-stu-id="03905-125">You can suppress a warning from this rule if you are sure that the dereferenced parameter has been validated by another method call in the function.</span></span>

## <a name="configure-code-to-analyze"></a><span data-ttu-id="03905-126">Konfigurowanie kodu do analizy</span><span class="sxs-lookup"><span data-stu-id="03905-126">Configure code to analyze</span></span>

<span data-ttu-id="03905-127">Użyj następujących opcji, aby skonfigurować, które części bazy kodu mają uruchamiać tę regułę.</span><span class="sxs-lookup"><span data-stu-id="03905-127">Use the following options to configure which parts of your codebase to run this rule on.</span></span>

- [<span data-ttu-id="03905-128">Wyklucz określone symbole</span><span class="sxs-lookup"><span data-stu-id="03905-128">Exclude specific symbols</span></span>](#exclude-specific-symbols)
- [<span data-ttu-id="03905-129">Wyklucz określone typy i ich typy pochodne</span><span class="sxs-lookup"><span data-stu-id="03905-129">Exclude specific types and their derived types</span></span>](#exclude-specific-types-and-their-derived-types)
- [<span data-ttu-id="03905-130">Wyklucz metodę rozszerzenia "This"</span><span class="sxs-lookup"><span data-stu-id="03905-130">Exclude extension method 'this' parameter</span></span>](#exclude-extension-method-this-parameter)
- [<span data-ttu-id="03905-131">Metody sprawdzania poprawności sprawdzania wartości null</span><span class="sxs-lookup"><span data-stu-id="03905-131">Null check validation methods</span></span>](#null-check-validation-methods)

<span data-ttu-id="03905-132">Te opcje można skonfigurować tylko dla tej reguły, dla wszystkich reguł lub dla wszystkich reguł w tej kategorii (projekt).</span><span class="sxs-lookup"><span data-stu-id="03905-132">These options can be configured for just this rule, for all rules, or for all rules in this category (Design).</span></span> <span data-ttu-id="03905-133">Aby uzyskać więcej informacji, zobacz [Opcje konfiguracji reguł jakości kodu](../code-quality-rule-options.md).</span><span class="sxs-lookup"><span data-stu-id="03905-133">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

[!INCLUDE[excluded-symbol-names](~/includes/code-analysis/excluded-symbol-names.md)]

[!INCLUDE[excluded-type-names-with-derived-types](~/includes/code-analysis/excluded-type-names-with-derived-types.md)]

### <a name="exclude-extension-method-this-parameter"></a><span data-ttu-id="03905-134">Wyklucz metodę rozszerzenia "This"</span><span class="sxs-lookup"><span data-stu-id="03905-134">Exclude extension method 'this' parameter</span></span>

<span data-ttu-id="03905-135">Domyślnie ta reguła analizuje i flaguje `this` parametr dla metod rozszerzających.</span><span class="sxs-lookup"><span data-stu-id="03905-135">By default, this rule analyzes and flags the `this` parameter for extension methods.</span></span> <span data-ttu-id="03905-136">Możesz wykluczyć analizę `this` parametrów dla metod rozszerzających, dodając następującą parę klucz-wartość do pliku *editorconfig* w projekcie:</span><span class="sxs-lookup"><span data-stu-id="03905-136">You can exclude analysis of the `this` parameter for extension methods by adding the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.exclude_extension_method_this_parameter = true
```

### <a name="null-check-validation-methods"></a><span data-ttu-id="03905-137">Metody sprawdzania poprawności sprawdzania wartości null</span><span class="sxs-lookup"><span data-stu-id="03905-137">Null check validation methods</span></span>

<span data-ttu-id="03905-138">Ta reguła może prowadzić do fałszywych wartości dodatnich, jeśli kod wywołuje specjalne metody sprawdzania poprawności sprawdzania wartości null w przywoływanych bibliotekach lub projektach.</span><span class="sxs-lookup"><span data-stu-id="03905-138">This rule can lead to false positives if your code calls special null check validation methods in referenced libraries or projects.</span></span> <span data-ttu-id="03905-139">Można uniknąć tych fałszywych wartości dodatnich przez określenie nazwy lub sygnatury metod sprawdzania poprawności sprawdzenia wartości null.</span><span class="sxs-lookup"><span data-stu-id="03905-139">You can avoid these false positives by specifying the name or signature of null check validation methods.</span></span>  <span data-ttu-id="03905-140">Następnie analiza przyjmie, że argumenty przekazane do tej metody są inne niż null po wywołaniu.</span><span class="sxs-lookup"><span data-stu-id="03905-140">The analysis will then assume that arguments passed to this method are non-null after the call.</span></span> <span data-ttu-id="03905-141">Na przykład aby oznaczyć wszystkie metody o nazwie `Validate` jako metody sprawdzania poprawności sprawdzenia wartości null, można dodać następującą parę klucz-wartość do pliku *editorconfig* w projekcie:</span><span class="sxs-lookup"><span data-stu-id="03905-141">For example, to mark all methods named `Validate` as null check validation methods, you can add the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.null_check_validation_methods = Validate
```

<span data-ttu-id="03905-142">Dozwolone formaty nazw metod w wartości opcji (oddzielonej przez `|` ):</span><span class="sxs-lookup"><span data-stu-id="03905-142">Allowed method name formats in the option value (separated by `|`):</span></span>

- <span data-ttu-id="03905-143">Tylko nazwa metody (zawiera wszystkie metody o nazwie, niezależnie od typu zawierającego lub przestrzeni nazw)</span><span class="sxs-lookup"><span data-stu-id="03905-143">Method name only (includes all methods with the name, regardless of the containing type or namespace)</span></span>
- <span data-ttu-id="03905-144">W pełni kwalifikowane nazwy w [formacie identyfikatora dokumentacji](../../../csharp/programming-guide/xmldoc/processing-the-xml-file.md#id-strings)symbolu z opcjonalnym `M:` prefiksem.</span><span class="sxs-lookup"><span data-stu-id="03905-144">Fully qualified names in the symbol's [documentation ID format](../../../csharp/programming-guide/xmldoc/processing-the-xml-file.md#id-strings), with an optional `M:` prefix.</span></span>

<span data-ttu-id="03905-145">Przykłady:</span><span class="sxs-lookup"><span data-stu-id="03905-145">Examples:</span></span>

| <span data-ttu-id="03905-146">Wartość opcji</span><span class="sxs-lookup"><span data-stu-id="03905-146">Option Value</span></span> | <span data-ttu-id="03905-147">Podsumowanie</span><span class="sxs-lookup"><span data-stu-id="03905-147">Summary</span></span> |
| --- | --- |
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate` | <span data-ttu-id="03905-148">Dopasowuje wszystkie metody nazwane `Validate` w kompilacji</span><span class="sxs-lookup"><span data-stu-id="03905-148">Matches all methods named `Validate` in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate1|Validate2` | <span data-ttu-id="03905-149">Dopasowuje wszystkie metody o nazwie `Validate1` lub `Validate2` w kompilacji</span><span class="sxs-lookup"><span data-stu-id="03905-149">Matches all methods named either `Validate1` or `Validate2` in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS.MyType.Validate(ParamType)` | <span data-ttu-id="03905-150">Dopasowuje określoną metodę `Validate` przy użyciu podanego w pełni kwalifikowanego podpisu</span><span class="sxs-lookup"><span data-stu-id="03905-150">Matches specific method `Validate` with given fully qualified signature</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS1.MyType1.Validate1(ParamType)|NS2.MyType2.Validate2(ParamType)` | <span data-ttu-id="03905-151">Dopasowuje określone metody `Validate1` i `Validate2` z odpowiednimi w pełni kwalifikowanymi sygnaturami</span><span class="sxs-lookup"><span data-stu-id="03905-151">Matches specific methods `Validate1` and `Validate2` with respective fully qualified signature</span></span>

## <a name="example-1"></a><span data-ttu-id="03905-152">Przykład 1</span><span class="sxs-lookup"><span data-stu-id="03905-152">Example 1</span></span>

<span data-ttu-id="03905-153">Poniższy przykład pokazuje metodę, która narusza regułę i metodę, która spełnia kryteria.</span><span class="sxs-lookup"><span data-stu-id="03905-153">The following example shows a method that violates the rule and a method that satisfies the rule.</span></span>

```csharp
using System;

namespace DesignLibrary
{
    public class Test
    {
        // This method violates the rule.
        public void DoNotValidate(string input)
        {
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }

        // This method satisfies the rule.
        public void Validate(string input)
        {
            if (input == null)
            {
                throw new ArgumentNullException(nameof(input));
            }
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }
    }
}
```

```vb
Imports System

Namespace DesignLibrary

    Public Class Test

        ' This method violates the rule.
        Sub DoNotValidate(ByVal input As String)

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

        ' This method satisfies the rule.
        Sub Validate(ByVal input As String)

            If input Is Nothing Then
                Throw New ArgumentNullException(NameOf(input))
            End If

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

    End Class

End Namespace
```

## <a name="example-2"></a><span data-ttu-id="03905-154">Przykład 2</span><span class="sxs-lookup"><span data-stu-id="03905-154">Example 2</span></span>

<span data-ttu-id="03905-155">Konstruktory kopiujące wypełniające pola lub właściwości, które są obiektami odniesienia, mogą również naruszać regułę CA1062.</span><span class="sxs-lookup"><span data-stu-id="03905-155">Copy constructors that populate field or properties that are reference objects can also violate the CA1062 rule.</span></span> <span data-ttu-id="03905-156">Naruszenie występuje, ponieważ skopiowany obiekt, który jest przesyłany do konstruktora kopiującego, może być `null` ( `Nothing` w Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="03905-156">The violation occurs because the copied object that is passed to the copy constructor might be `null` (`Nothing` in Visual Basic).</span></span> <span data-ttu-id="03905-157">Aby rozwiązać ten problem, należy użyć statycznej metody udostępnionej w Visual Basic), aby sprawdzić, czy skopiowany obiekt nie ma wartości null.</span><span class="sxs-lookup"><span data-stu-id="03905-157">To resolve the violation, use a static (Shared in Visual Basic) method to check that the copied object is not null.</span></span>

<span data-ttu-id="03905-158">W poniższym `Person` przykładzie klasy `other` obiekt, który jest przesyłany do `Person` konstruktora kopiującego może być `null` .</span><span class="sxs-lookup"><span data-stu-id="03905-158">In the following `Person` class example, the `other` object that is passed to the `Person` copy constructor might be `null`.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor CA1062 fires because other is dereferenced
    // without being checked for null
    public Person(Person other)
        : this(other.Name, other.Age)
    {
    }
}
```

## <a name="example-3"></a><span data-ttu-id="03905-159">Przykład 3</span><span class="sxs-lookup"><span data-stu-id="03905-159">Example 3</span></span>

<span data-ttu-id="03905-160">W poniższym, zmienionym `Person` przykładzie, `other` obiekt, który jest przesyłany do konstruktora kopiującego, jest najpierw sprawdzany pod kątem wartości null w `PassThroughNonNull` metodzie.</span><span class="sxs-lookup"><span data-stu-id="03905-160">In the following revised `Person` example, the `other` object that is passed to the copy constructor is first checked for null in the `PassThroughNonNull` method.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor
    public Person(Person other)
        : this(PassThroughNonNull(other).Name, other.Age)
    {
    }

    // Null check method
    private static Person PassThroughNonNull(Person person)
    {
        if (person == null)
            throw new ArgumentNullException(nameof(person));
        return person;
    }
}
```
