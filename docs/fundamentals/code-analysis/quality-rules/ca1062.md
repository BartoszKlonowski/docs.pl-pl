---
title: 'CA1062: Weryfikuj argumenty metod publicznych (analiza kodu)'
description: 'Dowiedz się więcej o regule analizy kodu CA1062: Weryfikuj argumenty metod publicznych'
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
- Validate arguments of public methods
helpviewer_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
author: gewarren
ms.author: gewarren
dev_langs:
- CSharp
- VB
ms.openlocfilehash: 74d6b9ea025f358e1d82290f9429ab99057aaaa6
ms.sourcegitcommit: 2e4adc490c1d2a705a0592b295d606b10b9f51f1
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/25/2020
ms.locfileid: "96589063"
---
# <a name="ca1062-validate-arguments-of-public-methods"></a><span data-ttu-id="a5a30-103">CA1062: Waliduj argumenty metod publicznych</span><span class="sxs-lookup"><span data-stu-id="a5a30-103">CA1062: Validate arguments of public methods</span></span>

| | <span data-ttu-id="a5a30-104">Wartość</span><span class="sxs-lookup"><span data-stu-id="a5a30-104">Value</span></span> |
|-|-|
| <span data-ttu-id="a5a30-105">**Identyfikator zasady**</span><span class="sxs-lookup"><span data-stu-id="a5a30-105">**Rule ID**</span></span> |<span data-ttu-id="a5a30-106">CA1062</span><span class="sxs-lookup"><span data-stu-id="a5a30-106">CA1062</span></span>|
| <span data-ttu-id="a5a30-107">**Kategoria**</span><span class="sxs-lookup"><span data-stu-id="a5a30-107">**Category**</span></span> |<span data-ttu-id="a5a30-108">Microsoft. Design</span><span class="sxs-lookup"><span data-stu-id="a5a30-108">Microsoft.Design</span></span>|
| <span data-ttu-id="a5a30-109">**Naprawa jest przerywana lub nieprzerwana**</span><span class="sxs-lookup"><span data-stu-id="a5a30-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="a5a30-110">Nieprzerwanie</span><span class="sxs-lookup"><span data-stu-id="a5a30-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="a5a30-111">Przyczyna</span><span class="sxs-lookup"><span data-stu-id="a5a30-111">Cause</span></span>

<span data-ttu-id="a5a30-112">Widoczna na zewnątrz Metoda odwołuje się do jednego z argumentów odwołania bez weryfikowania, czy ten argument jest `null` ( `Nothing` w Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="a5a30-112">An externally visible method dereferences one of its reference arguments without verifying whether that argument is `null` (`Nothing` in Visual Basic).</span></span>

<span data-ttu-id="a5a30-113">Następujące aspekty tej reguły [można konfigurować](#configurability):</span><span class="sxs-lookup"><span data-stu-id="a5a30-113">The following aspects of this rule are [configurable](#configurability):</span></span>

* <span data-ttu-id="a5a30-114">Określa, czy należy analizować metodę rozszerzenia "This".</span><span class="sxs-lookup"><span data-stu-id="a5a30-114">Whether or not to analyze extension method 'this' parameter.</span></span>
* <span data-ttu-id="a5a30-115">Określ metody sprawdzania poprawności sprawdzenia wartości null w przywoływanych bibliotekach lub projektach, które sprawdzają, czy argumenty przekazane do niego są inne niż null.</span><span class="sxs-lookup"><span data-stu-id="a5a30-115">Specify null check validation methods in referenced libraries or projects, which validate that arguments passed to it are non-null.</span></span>
* <span data-ttu-id="a5a30-116">Części bazy kodu do wykluczenia z analizy.</span><span class="sxs-lookup"><span data-stu-id="a5a30-116">Parts of codebase to exclude from analysis.</span></span>

## <a name="rule-description"></a><span data-ttu-id="a5a30-117">Opis reguły</span><span class="sxs-lookup"><span data-stu-id="a5a30-117">Rule description</span></span>

<span data-ttu-id="a5a30-118">Wszystkie argumenty odwołania, które są przekazane do widocznych zewnętrznie metod, powinny być sprawdzane względem `null` .</span><span class="sxs-lookup"><span data-stu-id="a5a30-118">All reference arguments that are passed to externally visible methods should be checked against `null`.</span></span> <span data-ttu-id="a5a30-119">W razie potrzeby throw, <xref:System.ArgumentNullException> gdy argument ma wartość `null` .</span><span class="sxs-lookup"><span data-stu-id="a5a30-119">If appropriate, throw a <xref:System.ArgumentNullException> when the argument is `null`.</span></span>

<span data-ttu-id="a5a30-120">Jeśli metodę można wywołać z nieznanego zestawu, ponieważ jest ona zadeklarowana jako publiczna lub chroniona, należy zweryfikować wszystkie parametry metody.</span><span class="sxs-lookup"><span data-stu-id="a5a30-120">If a method can be called from an unknown assembly because it is declared public or protected, you should validate all parameters of the method.</span></span> <span data-ttu-id="a5a30-121">Jeśli metoda jest przeznaczona do wywoływania tylko przez znane zestawy, należy wprowadzić metodę wewnętrzną i zastosować <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> atrybut do zestawu, który zawiera metodę.</span><span class="sxs-lookup"><span data-stu-id="a5a30-121">If the method is designed to be called only by known assemblies, you should make the method internal and apply the <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> attribute to the assembly that contains the method.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="a5a30-122">Jak naprawić naruszenia</span><span class="sxs-lookup"><span data-stu-id="a5a30-122">How to fix violations</span></span>

<span data-ttu-id="a5a30-123">Aby naprawić naruszenie tej reguły, sprawdź poprawność każdego argumentu odwołania względem `null` .</span><span class="sxs-lookup"><span data-stu-id="a5a30-123">To fix a violation of this rule, validate each reference argument against `null`.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="a5a30-124">Kiedy pominąć ostrzeżenia</span><span class="sxs-lookup"><span data-stu-id="a5a30-124">When to suppress warnings</span></span>

<span data-ttu-id="a5a30-125">Możesz pominąć ostrzeżenie z tej reguły, jeśli masz pewność, że odwołanie do parametru zostało zweryfikowane przez inne wywołanie metody w funkcji.</span><span class="sxs-lookup"><span data-stu-id="a5a30-125">You can suppress a warning from this rule if you are sure that the dereferenced parameter has been validated by another method call in the function.</span></span>

## <a name="configurability"></a><span data-ttu-id="a5a30-126">Określając</span><span class="sxs-lookup"><span data-stu-id="a5a30-126">Configurability</span></span>

<span data-ttu-id="a5a30-127">Ta reguła ma następujące konfigurowalne opcje.</span><span class="sxs-lookup"><span data-stu-id="a5a30-127">This rule has the following configurable options.</span></span>

### <a name="exclude-extension-method-this-parameter"></a><span data-ttu-id="a5a30-128">Wyklucz metodę rozszerzenia "This"</span><span class="sxs-lookup"><span data-stu-id="a5a30-128">Exclude extension method 'this' parameter</span></span>

<span data-ttu-id="a5a30-129">Domyślnie ta reguła analizuje i flaguje `this` parametr dla metod rozszerzających.</span><span class="sxs-lookup"><span data-stu-id="a5a30-129">By default, this rule analyzes and flags the `this` parameter for extension methods.</span></span> <span data-ttu-id="a5a30-130">Możesz wykluczyć analizę `this` parametrów dla metod rozszerzających, dodając następującą parę klucz-wartość do pliku *editorconfig* w projekcie:</span><span class="sxs-lookup"><span data-stu-id="a5a30-130">You can exclude analysis of the `this` parameter for extension methods by adding the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.exclude_extension_method_this_parameter = true
```

### <a name="null-check-validation-methods"></a><span data-ttu-id="a5a30-131">Metody sprawdzania poprawności sprawdzania wartości null</span><span class="sxs-lookup"><span data-stu-id="a5a30-131">Null check validation methods</span></span>

<span data-ttu-id="a5a30-132">Ta reguła może prowadzić do fałszywych wartości dodatnich, jeśli kod wywołuje specjalne metody sprawdzania poprawności sprawdzania wartości null w przywoływanych bibliotekach lub projektach.</span><span class="sxs-lookup"><span data-stu-id="a5a30-132">This rule can lead to false positives if your code calls special null check validation methods in referenced libraries or projects.</span></span> <span data-ttu-id="a5a30-133">Można uniknąć tych fałszywych wartości dodatnich przez określenie nazwy lub sygnatury metod sprawdzania poprawności sprawdzenia wartości null.</span><span class="sxs-lookup"><span data-stu-id="a5a30-133">You can avoid these false positives by specifying the name or signature of null check validation methods.</span></span>  <span data-ttu-id="a5a30-134">Następnie analiza przyjmie, że argumenty przekazane do tej metody są inne niż null po wywołaniu.</span><span class="sxs-lookup"><span data-stu-id="a5a30-134">The analysis will then assume that arguments passed to this method are non-null after the call.</span></span> <span data-ttu-id="a5a30-135">Na przykład aby oznaczyć wszystkie metody o nazwie `Validate` jako metody sprawdzania poprawności sprawdzenia wartości null, można dodać następującą parę klucz-wartość do pliku *editorconfig* w projekcie:</span><span class="sxs-lookup"><span data-stu-id="a5a30-135">For example, to mark all methods named `Validate` as null check validation methods, you can add the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.null_check_validation_methods = Validate
```

<span data-ttu-id="a5a30-136">Dozwolone formaty nazw metod w wartości opcji (oddzielonej przez `|` ):</span><span class="sxs-lookup"><span data-stu-id="a5a30-136">Allowed method name formats in the option value (separated by `|`):</span></span>

- <span data-ttu-id="a5a30-137">Tylko nazwa metody (zawiera wszystkie metody o nazwie, niezależnie od typu zawierającego lub przestrzeni nazw)</span><span class="sxs-lookup"><span data-stu-id="a5a30-137">Method name only (includes all methods with the name, regardless of the containing type or namespace)</span></span>
- <span data-ttu-id="a5a30-138">W pełni kwalifikowane nazwy w [formacie identyfikatora dokumentacji](https://github.com/dotnet/csharplang/blob/master/spec/documentation-comments.md#id-string-format)symbolu z opcjonalnym `M:` prefiksem.</span><span class="sxs-lookup"><span data-stu-id="a5a30-138">Fully qualified names in the symbol's [documentation ID format](https://github.com/dotnet/csharplang/blob/master/spec/documentation-comments.md#id-string-format), with an optional `M:` prefix.</span></span>

<span data-ttu-id="a5a30-139">Przykłady:</span><span class="sxs-lookup"><span data-stu-id="a5a30-139">Examples:</span></span>

| <span data-ttu-id="a5a30-140">Wartość opcji</span><span class="sxs-lookup"><span data-stu-id="a5a30-140">Option Value</span></span> | <span data-ttu-id="a5a30-141">Podsumowanie</span><span class="sxs-lookup"><span data-stu-id="a5a30-141">Summary</span></span> |
| --- | --- |
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate` | <span data-ttu-id="a5a30-142">Dopasowuje wszystkie metody o nazwie "Validate" w kompilacji</span><span class="sxs-lookup"><span data-stu-id="a5a30-142">Matches all methods named 'Validate' in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate1|Validate2` | <span data-ttu-id="a5a30-143">Dopasowuje wszystkie metody o nazwie "Validate1" lub "Validate2" w kompilacji</span><span class="sxs-lookup"><span data-stu-id="a5a30-143">Matches all methods named either 'Validate1' or 'Validate2' in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS.MyType.Validate(ParamType)` | <span data-ttu-id="a5a30-144">Dopasowuje określoną metodę "Validate" przy użyciu podanego w pełni kwalifikowanego podpisu</span><span class="sxs-lookup"><span data-stu-id="a5a30-144">Matches specific method 'Validate' with given fully qualified signature</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS1.MyType1.Validate1(ParamType)|NS2.MyType2.Validate2(ParamType)` | <span data-ttu-id="a5a30-145">Dopasowuje określone metody "Validate1" i "Validate2" z odpowiednimi w pełni kwalifikowanymi sygnaturami</span><span class="sxs-lookup"><span data-stu-id="a5a30-145">Matches specific methods 'Validate1' and 'Validate2' with respective fully qualified signature</span></span>

### <a name="excluded-symbol-names"></a><span data-ttu-id="a5a30-146">Wykluczone nazwy symboli</span><span class="sxs-lookup"><span data-stu-id="a5a30-146">Excluded symbol names</span></span>

<span data-ttu-id="a5a30-147">Można skonfigurować, które części bazy kodu mają być wykluczone z analizy.</span><span class="sxs-lookup"><span data-stu-id="a5a30-147">You can configure which parts of your codebase to exclude from analysis.</span></span> <span data-ttu-id="a5a30-148">Na przykład aby określić, że reguła nie powinna być uruchamiana na żadnym kodzie w typach o nazwie `MyType` , Dodaj następującą parę klucz-wartość do pliku *editorconfig* w projekcie:</span><span class="sxs-lookup"><span data-stu-id="a5a30-148">For example, to specify that the rule should not run on any code within types named `MyType`, add the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.excluded_symbol_names = MyType
```

<span data-ttu-id="a5a30-149">Dozwolone formaty nazw symboli w wartości opcji (oddzielone przez `|` ):</span><span class="sxs-lookup"><span data-stu-id="a5a30-149">Allowed symbol name formats in the option value (separated by `|`):</span></span>

- <span data-ttu-id="a5a30-150">Tylko nazwa symbolu (zawiera wszystkie symbole z nazwą, niezależnie od typu zawierającego lub przestrzeni nazw)</span><span class="sxs-lookup"><span data-stu-id="a5a30-150">Symbol name only (includes all symbols with the name, regardless of the containing type or namespace)</span></span>
- <span data-ttu-id="a5a30-151">W pełni kwalifikowane nazwy w [formacie identyfikatora dokumentacji](https://github.com/dotnet/csharplang/blob/master/spec/documentation-comments.md#id-string-format)symbolu.</span><span class="sxs-lookup"><span data-stu-id="a5a30-151">Fully qualified names in the symbol's [documentation ID format](https://github.com/dotnet/csharplang/blob/master/spec/documentation-comments.md#id-string-format).</span></span> <span data-ttu-id="a5a30-152">Każda nazwa symbolu wymaga prefiksu rodzaju symboli, takiego jak "M:" prefix dla metod, `T:` prefix dla typów, "N:" prefiks dla przestrzeni nazw itd.</span><span class="sxs-lookup"><span data-stu-id="a5a30-152">Each symbol name requires a symbol kind prefix, such as "M:" prefix for methods, `T:` prefix for types, "N:" prefix for namespaces, etc.</span></span>
- <span data-ttu-id="a5a30-153">`.ctor` dla konstruktorów i `.cctor` dla konstruktorów statycznych</span><span class="sxs-lookup"><span data-stu-id="a5a30-153">`.ctor` for constructors and `.cctor` for static constructors</span></span>

<span data-ttu-id="a5a30-154">Przykłady:</span><span class="sxs-lookup"><span data-stu-id="a5a30-154">Examples:</span></span>

| <span data-ttu-id="a5a30-155">Wartość opcji</span><span class="sxs-lookup"><span data-stu-id="a5a30-155">Option Value</span></span> | <span data-ttu-id="a5a30-156">Podsumowanie</span><span class="sxs-lookup"><span data-stu-id="a5a30-156">Summary</span></span> |
| --- | --- |
|`dotnet_code_quality.CA1062.excluded_symbol_names = MyType` | <span data-ttu-id="a5a30-157">Dopasowuje wszystkie symbole o nazwie "MyType" w kompilacji</span><span class="sxs-lookup"><span data-stu-id="a5a30-157">Matches all symbols named 'MyType' in the compilation</span></span>
|`dotnet_code_quality.CA1062.excluded_symbol_names = MyType1|MyType2` | <span data-ttu-id="a5a30-158">Dopasowuje wszystkie symbole o nazwie "MyType1" lub "MyType2" w kompilacji</span><span class="sxs-lookup"><span data-stu-id="a5a30-158">Matches all symbols named either 'MyType1' or 'MyType2' in the compilation</span></span>
|`dotnet_code_quality.CA1062.excluded_symbol_names = M:NS.MyType.MyMethod(ParamType)` | <span data-ttu-id="a5a30-159">Dopasowuje określoną metodę "nomethod" przy użyciu podanego w pełni kwalifikowanego podpisu</span><span class="sxs-lookup"><span data-stu-id="a5a30-159">Matches specific method 'MyMethod' with given fully qualified signature</span></span>
|`dotnet_code_quality.CA1062.excluded_symbol_names = M:NS1.MyType1.MyMethod1(ParamType)|M:NS2.MyType2.MyMethod2(ParamType)` | <span data-ttu-id="a5a30-160">Dopasowuje określone metody "MyMethod1" i "MyMethod2" z odpowiednimi w pełni kwalifikowanymi sygnaturami</span><span class="sxs-lookup"><span data-stu-id="a5a30-160">Matches specific methods 'MyMethod1' and 'MyMethod2' with respective fully qualified signature</span></span>

<span data-ttu-id="a5a30-161">Wszystkie te opcje można skonfigurować dla opcji tylko dla tej reguły, dla wszystkich reguł lub dla wszystkich reguł w tej kategorii (projekt).</span><span class="sxs-lookup"><span data-stu-id="a5a30-161">You can configure all of these options for just this rule, for all rules, or for all rules in this category (Design).</span></span> <span data-ttu-id="a5a30-162">Aby uzyskać więcej informacji, zobacz [Opcje konfiguracji reguł jakości kodu](../code-quality-rule-options.md).</span><span class="sxs-lookup"><span data-stu-id="a5a30-162">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

## <a name="example-1"></a><span data-ttu-id="a5a30-163">Przykład 1</span><span class="sxs-lookup"><span data-stu-id="a5a30-163">Example 1</span></span>

<span data-ttu-id="a5a30-164">Poniższy przykład pokazuje metodę, która narusza regułę i metodę, która spełnia kryteria.</span><span class="sxs-lookup"><span data-stu-id="a5a30-164">The following example shows a method that violates the rule and a method that satisfies the rule.</span></span>

```csharp
using System;

namespace DesignLibrary
{
    public class Test
    {
        // This method violates the rule.
        public void DoNotValidate(string input)
        {
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }

        // This method satisfies the rule.
        public void Validate(string input)
        {
            if (input == null)
            {
                throw new ArgumentNullException(nameof(input));
            }
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }
    }
}
```

```vb
Imports System

Namespace DesignLibrary

    Public Class Test

        ' This method violates the rule.
        Sub DoNotValidate(ByVal input As String)

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

        ' This method satisfies the rule.
        Sub Validate(ByVal input As String)

            If input Is Nothing Then
                Throw New ArgumentNullException(NameOf(input))
            End If

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

    End Class

End Namespace
```

## <a name="example-2"></a><span data-ttu-id="a5a30-165">Przykład 2</span><span class="sxs-lookup"><span data-stu-id="a5a30-165">Example 2</span></span>

<span data-ttu-id="a5a30-166">Konstruktory kopiujące wypełniające pola lub właściwości, które są obiektami odniesienia, mogą również naruszać regułę CA1062.</span><span class="sxs-lookup"><span data-stu-id="a5a30-166">Copy constructors that populate field or properties that are reference objects can also violate the CA1062 rule.</span></span> <span data-ttu-id="a5a30-167">Naruszenie występuje, ponieważ skopiowany obiekt, który jest przesyłany do konstruktora kopiującego, może być `null` ( `Nothing` w Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="a5a30-167">The violation occurs because the copied object that is passed to the copy constructor might be `null` (`Nothing` in Visual Basic).</span></span> <span data-ttu-id="a5a30-168">Aby rozwiązać ten problem, należy użyć statycznej metody udostępnionej w Visual Basic), aby sprawdzić, czy skopiowany obiekt nie ma wartości null.</span><span class="sxs-lookup"><span data-stu-id="a5a30-168">To resolve the violation, use a static (Shared in Visual Basic) method to check that the copied object is not null.</span></span>

<span data-ttu-id="a5a30-169">W poniższym `Person` przykładzie klasy `other` obiekt, który jest przesyłany do `Person` konstruktora kopiującego może być `null` .</span><span class="sxs-lookup"><span data-stu-id="a5a30-169">In the following `Person` class example, the `other` object that is passed to the `Person` copy constructor might be `null`.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor CA1062 fires because other is dereferenced
    // without being checked for null
    public Person(Person other)
        : this(other.Name, other.Age)
    {
    }
}
```

## <a name="example-3"></a><span data-ttu-id="a5a30-170">Przykład 3</span><span class="sxs-lookup"><span data-stu-id="a5a30-170">Example 3</span></span>

<span data-ttu-id="a5a30-171">W poniższym, zmienionym `Person` przykładzie, `other` obiekt, który jest przesyłany do konstruktora kopiującego, jest najpierw sprawdzany pod kątem wartości null w `PassThroughNonNull` metodzie.</span><span class="sxs-lookup"><span data-stu-id="a5a30-171">In the following revised `Person` example, the `other` object that is passed to the copy constructor is first checked for null in the `PassThroughNonNull` method.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor
    public Person(Person other)
        : this(PassThroughNonNull(other).Name, other.Age)
    {
    }

    // Null check method
    private static Person PassThroughNonNull(Person person)
    {
        if (person == null)
            throw new ArgumentNullException(nameof(person));
        return person;
    }
}
```
